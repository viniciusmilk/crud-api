// O módulo é exportado com uma configuração específica
module.exports = {
    // A propriedade 'extends' é usada para estender as regras de um conjunto de configurações existente
    // Neste caso, está estendendo as regras do '@commitlint/config-conventional'
    // '@commitlint/config-conventional' é um conjunto de regras convencionais que são comumente usadas em muitos projetos
    extends: ['@commitlint/config-conventional']
  };